Console.WriteLine("start");
int [] CreateArray(int size);
{
    return new int[size];
}
{
Fill("int[] array, int index, int sum");
}
    ConsoleWriteLine("index = 0, sum = 0");
while (index <= size - 1)
{
    if (index[array] % 2 == 0)
    ConsoleWriteLine("sum = sum + array[index]");
}
index = index + 1;
ConsoleWriteLine("sum");